# subscription-manager register --username <username> --password <password> --auto-attach

in seconds below
$ gsettings set org.gnome.desktop.session idle-delay 3600
$ gsettings set org.gnome.desktop.screensaver lock-delay 3600
$ gsettings set org.gnome.desktop.screensaver lock-enabled true

/etc/default/grub and change following three lines;

GRUB_CMDLINE_LINUX="crashkernel=auto rd.lvm.lv=ol/root rd.lvm.lv=ol/swap console=tty1 console=ttyS0,115200"
GRUB_SERIAL_COMMAND="serial --unit=0 --speed=115200"
GRUB_TERMINAL="serial console"

cat /etc/default/grub

# grub2-mkconfig -o /boot/grub2/grub.cfg
# grep ttyS0 /etc/securetty
ttyS0

make sure VM has only one interface and create following file. Network interface enp0s3 which is default in VirtualBox doesnt exist in OCI KVM.

# cat /etc/sysconfig/network-scripts/ifcfg-ens3

# Generated by dracut initrd
NAME="ens3"
DEVICE="ens3"
ONBOOT=yes
NETBOOT=yes
BOOTPROTO=dhcp
TYPE=Ethernet
NM_CONTROLLED=no


Make sure the disks are addressed by UUID, LABEL or logical vollume name.
# cat /etc/fstab

#
# /etc/fstab
# Created by anaconda on Thu Jun 7 03:56:27 2018
#
# Accessible filesystems, by reference, are maintained under '/dev/disk'
# See man pages fstab(5), findfs(8), mount(8) and/or blkid(8) for more info
#
/dev/mapper/ol-root / xfs defaults 0 0
UUID=6f8d9940-590e-4b54-944f-2e3a4713459e /boot xfs defaults 0 0
/dev/mapper/ol-swap swap swap defaults 0 0


# cat .ssh/authorized_keys
ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQDgUlQrZb<some parts removed>VXZFAO8Dp oracle cloud logins